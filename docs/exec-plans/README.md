# Exec Plans

- `active/`: 진행 중인 계획
- `completed/`: 완료된 계획
- `tech-debt-tracker.md`: 알려진 부채와 후속 작업

운영 원칙:

- 체크리스트와 검증 결과가 모두 충족되고 추가 `Open Work`가 없으면 `active/`에서 `completed/`로 이동한다.
- 완료 문서로 이동할 때는 이를 참조하는 인덱스, `HANDOFF`, 검증 스크립트도 함께 갱신한다.
